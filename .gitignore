# Python
__pycache__/
*.py[cod]
*$py.class
.env
.venv/
env/
venv/
ENV/

# Distribution / packaging
dist/
build/
# ...except these: committed fixture helpers, not build output
!tests/fixtures/build/
*.egg-info/

# CEP / Web
node_modules/
.DS_Store

# Project specific
# Runtime data dir: HF model cache (GBs), bundled ffmpeg, media cache, sqlite,
# logs, runtime plan output, proof/diagnostic scripts. Never committed.
data/
logs/
*.log
*.sqlite3-shm
*.sqlite3-wal
*.sqlite3

# Sample/experiment media — keep source videos out of the repo
experiments/
*.mp4
*.mov
*.wav
!tests/fixtures/**/*.wav
