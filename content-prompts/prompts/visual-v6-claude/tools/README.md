# Visual Generation v6 Tools

Organized runtime helpers for the Claude v6 visual pipeline.

```text
browser/   Shared Edge CDP bootstrap, scouts, and browser task runner
dreamina/  Dreamina GPT Image 2 probing and generated-image download helpers
routing/   Tool/capability routing helper
```

The root `tools/` copies are kept for backward compatibility with existing prompt paths.
